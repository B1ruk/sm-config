spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/baseSchool    
  jpa:
    properties:
      hibernate:
        dialect:org.hibernate.dialect.MySQL5Dialect
    show-sql: true